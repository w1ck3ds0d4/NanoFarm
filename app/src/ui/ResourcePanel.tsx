// legacy panel. resource display now lives inline in App.tsx as the overlay
// bar plus the materials breakdown popover. kept as an empty export so future
// reintroduction does not need to recreate the filename.
export {};
